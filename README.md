Originally a prototype for the Juniper Dev game jam.

This project uses a custom attach point component to allow objects to be picked up at connected to each other using Unreal's physics constraints.
